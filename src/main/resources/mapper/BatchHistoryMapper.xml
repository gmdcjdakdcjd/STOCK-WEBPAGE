<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.stock.webpage.mapper.BatchHistoryMapper">

    <!-- 날짜 목록 조회 -->
    <select id="selectHistoryDateList"
            resultType="com.stock.webpage.dto.BatchDateGroupDTO">

        SELECT
        exec_date AS execDate,
        COUNT(*)  AS totalCount,
        SUM(CASE WHEN exec_status = 'FAIL' THEN 1 ELSE 0 END) AS failCount
        FROM batch_history_v
        <where>
            <if test="jobName != null and jobName != ''">
                job_name = #{jobName}
            </if>
        </where>
        GROUP BY exec_date
        ORDER BY exec_date DESC
        LIMIT #{offset}, #{size}

    </select>

    <!-- 날짜 목록 전체 개수 -->
    <select id="selectHistoryDateTotalCount"
            resultType="int">

        SELECT COUNT(DISTINCT exec_date)
        FROM batch_history_v
        <where>
            <if test="jobName != null and jobName != ''">
                job_name = #{jobName}
            </if>
        </where>

    </select>

    <!-- 특정 날짜 상세 조회 -->
    <select id="selectHistoryByDate"
            resultType="com.stock.webpage.dto.BatchHistoryView">

        SELECT
        exec_date      AS execDate,
        type           AS type,
        hist_id        AS histId,
        job_id         AS jobId,
        job_name       AS jobName,
        exec_status    AS execStatus,
        exec_message   AS execMessage,
        exec_start_time as execStartTime,
        exec_end_time   as execEndTime,
        duration_ms     as durationMs
        FROM batch_history_v
        WHERE exec_date = #{date}
        ORDER BY exec_start_time DESC
        LIMIT #{offset}, #{size}

    </select>

    <!-- 특정 날짜 전체 개수 -->
    <select id="selectHistoryDetailTotalCount"
            resultType="int">

        SELECT COUNT(*)
        FROM batch_history_v
        WHERE exec_date = #{date}

    </select>

    <!-- 배치 작업 설정 목록 조회 (IN) -->
    <select id="selectBatchInJobList" resultType="com.stock.webpage.dto.BatchJobDTO">
        SELECT
            'IN'            AS type,
            job_id          AS jobId,
            job_name        AS jobName,
            job_info        AS jobInfo,
            schedule_gb     AS scheduleGb,
            job_month       AS jobMonth,
            job_day         AS jobDay,
            job_week        AS jobWeek,
            job_hour        AS jobHour,
            job_min         AS jobMin,
            act_gb          AS actGb,
            last_exec_info  AS lastExecInfo,
            next_exec_info  AS nextExecInfo,
            is_active       AS isActive
        FROM batch_in
        ORDER BY job_id
    </select>

    <!-- 배치 작업 설정 목록 조회 (OUT) -->
    <select id="selectBatchOutJobList" resultType="com.stock.webpage.dto.BatchJobDTO">
        SELECT
            'OUT'           AS type,
            job_id          AS jobId,
            job_name        AS jobName,
            job_info        AS jobInfo,
            schedule_gb     AS scheduleGb,
            job_month       AS jobMonth,
            job_day         AS jobDay,
            job_week        AS jobWeek,
            job_hour        AS jobHour,
            job_min         AS jobMin,
            act_gb          AS actGb,
            last_exec_info  AS lastExecInfo,
            next_exec_info  AS nextExecInfo,
            is_active       AS isActive
        FROM batch_out
        ORDER BY job_id
    </select>

    <!-- BatchIn 상태 업데이트 -->
    <update id="updateBatchInActGb">
        UPDATE batch_in
        SET act_gb = #{actGb}
        WHERE job_id = #{jobId}
    </update>

    <!-- BatchIn 강제 재처리 시간 갱신 (2분 뒤 실행 예약) -->
    <update id="updateBatchInTriggerTime">
        UPDATE batch_in
        SET act_gb = 'N',
            job_hour = #{jobHour},
            job_min = #{jobMin}
        WHERE job_id = #{jobId}
    </update>

    <!-- BatchOut 상태 업데이트 -->
    <update id="updateBatchOutActGb">
        UPDATE batch_out
        SET act_gb = #{actGb}
        WHERE job_id = #{jobId}
    </update>

    <!-- 대기열(Queue)에 배치 작업 등록 -->
    <insert id="insertStockJobQueue">
        INSERT INTO stock_job_queue (
            job_code,
            status,
            batch_out_id
        ) VALUES (
            #{jobCode},
            #{status},
            #{batchOutId}
        )
    </insert>

    <!-- 특정 배치 Job명 기준 최근 이력 목록 조회 -->
    <select id="selectHistoryByJobName" resultType="com.stock.webpage.dto.BatchHistoryView">
        SELECT
            exec_date      AS execDate,
            type           AS type,
            hist_id        AS histId,
            job_id         AS jobId,
            job_name       AS jobName,
            exec_status    AS execStatus,
            exec_message   AS execMessage,
            exec_start_time as execStartTime,
            exec_end_time   as execEndTime,
            duration_ms     as durationMs
        FROM batch_history_v
        WHERE job_name = #{jobName}
        ORDER BY exec_start_time DESC
        LIMIT #{size}
    </select>

    <!-- 배치 스케줄(IN) 정보 업데이트 -->
    <update id="updateBatchInSchedule">
        UPDATE batch_in
        SET
            schedule_gb = #{scheduleGb},
            job_hour = #{jobHour},
            job_min = #{jobMin},
            job_week = #{jobWeek},
            job_day = #{jobDay},
            job_month = #{jobMonth}
        WHERE job_id = #{jobId}
    </update>

    <!-- 배치 스케줄(OUT) 정보 업데이트 -->
    <update id="updateBatchOutSchedule">
        UPDATE batch_out
        SET
            schedule_gb = #{scheduleGb},
            job_hour = #{jobHour},
            job_min = #{jobMin},
            job_week = #{jobWeek},
            job_day = #{jobDay},
            job_month = #{jobMonth}
        WHERE job_id = #{jobId}
    </update>

</mapper>